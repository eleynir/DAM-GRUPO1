<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/logo4"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo4"
        android:supportsRtl="true"
        android:theme="@style/Theme.BolsaTrabajoApp">
        <activity
            android:name=".AgregarCategoriaActivity"
            android:exported="false" />
        <activity
            android:name=".PublicarOfertaActivity"
            android:exported="false"
            android:theme="@style/Theme.BolsaTrabajoApp" />
        <activity
            android:name=".ListadoOfertasActivity"
            android:exported="false" />
        <activity
            android:name=".HomeActivity"
            android:exported="false" />
        <activity
            android:name=".RegistroActivity"
            android:exported="false"
            android:windowSoftInputMode="stateHidden|adjustResize" />
        <activity android:name=".PerfilActivity" />
        <activity
            android:name=".AccesoActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>